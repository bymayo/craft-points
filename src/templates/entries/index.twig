{% extends '_layouts/elementindex' %}

{% set title = 'Entries'|t('points') %}
{% set elementType = 'bymayo\\points\\elements\\PointEntry' %}
{% set selectedSubnavItem = 'entries' %}

{% set crumbs = [
    { label: 'Points'|t('points'), url: url('points') },
] %}

{% block actionButton %}
    <a href="{{ url('points/entries/new') }}" class="btn submit add icon">
        {{ 'New entry'|t('points') }}
    </a>
{% endblock %}
