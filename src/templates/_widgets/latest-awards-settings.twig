{% import '_includes/forms.twig' as forms %}

{{ forms.textField({
    label: 'Limit'|t('points'),
    id: 'limit',
    name: 'limit',
    type: 'number',
    value: widget.limit,
    errors: widget.getErrors('limit'),
}) }}
