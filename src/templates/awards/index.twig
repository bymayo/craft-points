{% extends '_layouts/elementindex' %}

{% set title = 'Awards'|t('points') %}
{% set elementType = 'bymayo\\points\\elements\\PointAward' %}
{% set selectedSubnavItem = 'awards' %}

{% set crumbs = [
    { label: craft.points.currencyPlural, url: url('points') },
] %}

{% block actionButton %}
    <a href="{{ url('points/awards/new') }}" class="btn submit add icon">
        {{ 'New award'|t('points') }}
    </a>
{% endblock %}
